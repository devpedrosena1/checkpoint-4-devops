CREATE TABLE USUARIO_CP (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOME VARCHAR2(100) NOT NULL,
    EMAIL VARCHAR2(100) UNIQUE NOT NULL
);

CREATE TABLE CONTATO_CP (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TELEFONE VARCHAR2(20) NOT NULL,
    USUARIO_ID NUMBER,
    CONSTRAINT FK_CONTATO_USUARIO FOREIGN KEY (USUARIO_ID)
        REFERENCES USUARIO_CP(ID)
);

SELECT * FROM USUARIO_CP;
SELECT * FROM CONTATO_CP;